@model RealEstate.Models.RealEstateObjectViewModel

@{
    ViewData["Title"] = "Details";
}

<div class="row">
    <div class="container text-center">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <!-- Portfolio Modal - Title -->
                <h2 class="portfolio-modal-title text-secondary text-uppercase mb-0">@Model.Object.Name</h2>
                <!-- Icon Divider -->
                <div class="divider-custom">
                    <div class="divider-custom-line"></div>
                    <div class="divider-custom-icon">
                        <i class="fas fa-star"></i>
                    </div>
                    <div class="divider-custom-line"></div>
                </div>
                <!-- Portfolio Modal - Image -->
                <img class="img-fluid rounded mb-5" src="@Model.ImageUrl" alt="">
                <!-- Portfolio Modal - Text -->
                <p>
                    Number of bathrooms: @Model.Object.NumberOfBathrooms<br />
                    Number of bedrooms: @Model.Object.NumberOfBedrooms<br />
                    <b>Price: @Model.Object.Price</b>
                </p>
                <p class="mb-5">
                    @Model.Object.Description
                </p>
            </div>
        </div>
    </div>
</div>